ext {
  androidxAppCompatVersion = '1.3.1'  // Reemplaza con la versión que necesitas
  androidxCoordinatorLayoutVersion = '1.1.0'
  coreSplashScreenVersion = '1.0.0'
  junitVersion = '4.13.2'  // Agregamos la versión de JUnit aquí
  androidxJunitVersion = '1.1.3'  // Agregamos la versión de androidx JUnit aquí
  androidxEspressoCoreVersion = '3.4.0'  // Asegúrate de definir esta variable si la estás utilizando en otro lugar
}
