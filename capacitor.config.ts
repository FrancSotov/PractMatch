import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'pract.match',
  appName: 'PractMatch',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
